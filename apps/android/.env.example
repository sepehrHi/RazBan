# Copy this file to .env and fill in real values.
# Currently unused by app code (worker URL is entered by the user at runtime),
# but the Secrets Gradle Plugin requires this file to exist to configure BuildConfig.
#
# MAPS_API_KEY=
